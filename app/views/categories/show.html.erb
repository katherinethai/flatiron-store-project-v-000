<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
  <p><%= item.title %> | $<%= item.price.to_f %>0 | <%= button_to 'Add to Cart', line_items_path(item_id: item) if user_signed_in? %></p>

<% end %>
</ul>